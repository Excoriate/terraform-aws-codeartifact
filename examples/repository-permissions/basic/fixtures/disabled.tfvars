# Disabled fixture: Sets the main is_enabled flag to false.
# This should prevent the creation of the domain, repository module,
# and repository-permissions module resources defined in main.tf.

is_enabled = false
