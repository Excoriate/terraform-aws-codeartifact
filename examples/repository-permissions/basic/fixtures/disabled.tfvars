# Disabled fixture - sets is_enabled to false to prevent policy creation/management.
is_enabled = false

# domain_name and repository_name are still required by the example's variables.tf
# Replace placeholders with actual existing resource names if needed for other potential example resources (though unlikely here).
domain_name     = "your-existing-domain"
repository_name = "your-existing-repo"
