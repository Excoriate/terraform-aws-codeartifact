# This fixture disables the domain-permissions module call within the example.
is_enabled = false

# domain_name_suffix is still needed for consistency, even if not used when disabled.
domain_name_suffix = "dynamic-disabled"
