# Disabled fixture - sets is_enabled to false to prevent policy creation/management.
is_enabled = false

# domain_name is still required by the example's variables.tf
# Replace "your-existing-domain" with an actual existing domain name if needed for other potential example resources.
domain_name = "your-existing-domain"
